apiVersion: v1
kind: Service
metadata:
  name: ecommerce-backend-service
  namespace: ecommerce
spec:
  selector:
    app: ecommerce-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
